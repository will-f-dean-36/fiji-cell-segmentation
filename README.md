# Fiji Cell Segmentation

Fiji/ImageJ plugin for segmenting cells using edge detection, thresholding, and watershed.

## Status
In active development

## Build
```bash
mvn -DskipTests package

## Install
Copy the generated JAR from `target/` into Fiji `plugins/`
